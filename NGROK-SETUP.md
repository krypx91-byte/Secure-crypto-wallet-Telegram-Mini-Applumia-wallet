# 🚀 Инструкция: Тестирование в Telegram через ngrok

## Шаг 1: Установка ngrok

### Вариант A: Автоматическая установка (Chocolatey)
```powershell
choco install ngrok
```

### Вариант B: Ручная установка (рекомендую)

1. **Скачайте ngrok:**
   - Откройте: https://ngrok.com/download
   - Выберите **Windows (64-bit)**
   - Скачайте ZIP файл

2. **Распакуйте:**
   - Распакуйте `ngrok.exe` в удобное место
   - Например: `C:\ngrok\ngrok.exe`

3. **Добавьте в PATH (опционально):**
   ```powershell
   # Или просто запускайте из папки где лежит ngrok.exe
   ```

## Шаг 2: Регистрация на ngrok (бесплатно)

1. Зарегистрируйтесь на https://dashboard.ngrok.com/signup
2. Получите ваш authtoken
3. Выполните:
   ```bash
   ngrok config add-authtoken ВАШ_ТОКЕН
   ```

## Шаг 3: Запуск ngrok

У вас уже запущен сервер на порту 8000 ✅

Теперь в **НОВОМ терминале** выполните:

```bash
# Если ngrok в PATH:
ngrok http 8000

# Или если ngrok.exe в папке C:\ngrok:
C:\ngrok\ngrok.exe http 8000
```

## Шаг 4: Получите HTTPS URL

После запуска ngrok покажет что-то вроде:

```
Forwarding    https://abc123.ngrok.io -> http://localhost:8000
```

**Скопируйте HTTPS URL!** (например: `https://abc123.ngrok.io`)

## Шаг 5: Создайте Telegram бота

1. **Откройте Telegram** и найдите [@BotFather](https://t.me/botfather)

2. **Создайте бота:**
   ```
   /newbot
   ```
   - Имя: `Lumia Wallet`
   - Username: `lumia_wallet_test_bot` (или любое свободное)

3. **Создайте Web App:**
   ```
   /newapp
   ```
   - Выберите вашего бота
   - Название: `Lumia Wallet`
   - Описание: `Secure crypto wallet`
   - Загрузите фото: `lumia-logo-final.png`
   - **URL**: `https://abc123.ngrok.io/lumia-wallet.html` (ваш ngrok URL!)
   - Короткое имя: `wallet`

4. **Настройте кнопку меню:**
   ```
   /setmenubutton
   ```
   - Выберите бота
   - Тип: `Web App`
   - Текст: `Open Wallet 💎`
   - URL: `https://abc123.ngrok.io/lumia-wallet.html`

## Шаг 6: Тестируйте! 🎉

1. Откройте вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку **"Open Wallet 💎"** в меню
4. Приложение откроется в Telegram!

## ✨ Что проверить:

- ✅ Тема синхронизируется с Telegram (светлая/тёмная)
- ✅ Кнопка MainButton появляется внизу
- ✅ BackButton работает для навигации
- ✅ Вибрация при нажатиях (HapticFeedback)
- ✅ Все экраны работают

## ⚠️ Важно:

- ngrok URL меняется при каждом перезапуске
- Для постоянного URL нужен GitHub Pages
- Бесплатный ngrok имеет ограничения по времени

## 🔄 Если ngrok URL изменился:

```
/editapp
```
- Выберите бота → Edit URL → введите новый ngrok URL

---

**Готово! Теперь у вас Telegram Mini App работает! 🚀**
